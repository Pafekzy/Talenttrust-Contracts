[package]
name = "escrow"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = []

[dependencies]
soroban-sdk = { version = "22.0.11", default-features = false }

[dev-dependencies]
soroban-sdk = { version = "22.0.11", features = ["testutils"] }
proptest = "1.10.0"